# AI Insurance Claim Reviewer (Reflection + RAG)

## Features
- PDF claim ingestion
- Policy-based reasoning (RAG)
- Reflection (critic + improvement)
- Multi-agent debate
- Streamlit UI

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
